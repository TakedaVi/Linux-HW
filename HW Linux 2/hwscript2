#!/bin/bash
tar cpf /archive/"home-backup created on `date '+%d-%B-%Y'`.tar" /home/ 
tar cpf /archive/"home-backup created on `date '+%d-%B-%Y'`.tar"/var/log/ 
tar cpf /archive/"home-backup created on `date '+%d-%B-%Y'`.tar"/etc/ssh/sshd_config /etc/vsftpd.conf /etc/xrdp/xrdp.ini
